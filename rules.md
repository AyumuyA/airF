# Antigravity CLI 開発ガイドライン (rules.md)

本プロジェクト（3D戦闘機ドッグファイト＆レースゲーム）における、Antigravity CLIの行動指針およびコーディングルールを定義する。仕様書（`spec.md`）と合わせて必ず参照し、以下のルールを厳守して開発をサポートすること。

## 1. 開発ワークフローとステップの細分化
* 一度に複数の機能や巨大なシステムを実装しようとせず、必ず1機能ずつ段階的に実装すること。
* 各ステップの実装後は、必ずブラウザやコンソールでエラーが出ない実行可能な状態を維持すること。
* 既存のコアロジック（特にThree.jsの描画ループやWebSocketの同期処理）に大きな変更を加える場合は、事前に作業方針を提示し、ユーザー（ogulux）にGitでのコミットを促すこと。

## 2. アーキテクチャとファイル分割（モジュール化）
* 全ての処理を1つのファイル（`main.js`や`server.js`等）に記述することを固く禁ずる。
* 以下の要素は、必ず別ファイルまたは独立したクラス/モジュールとして分割すること。
  * **フロントエンド:** 
    * Three.jsによる360度ビューのカメラ制御・描画ループ処理
    * 機体の物理演算（ピッチ、ヨー、ロールなどのクォータニオン計算）
    * UI/HUD処理（2Dレーダー、矢印ガイド、HPバー等のDOM操作やCanvas描画）
    * WebSocketクライアント通信ロジック
  * **バックエンド:** 
    * WebSocketサーバー通信ロジック
    * ロビー・ルーム管理、チーム状態の管理ロジック
* 特に、360度自由に見渡せるThree.jsのカメラ制御は計算が破綻しやすいため、他のロジックと密結合させないこと。

## 3. ネットワーク設定とデプロイを見据えた設計
* WebSocketの接続先IPアドレス、ポート番号などはコード内にハードコード（直書き）しないこと。
* 将来的なUbuntuサーバー環境へのデプロイや、ターミナルからのIPルーティング設定を想定し、ネットワーク関連の設定は必ず `config.js` や `.env` ファイル等の外部設定ファイルに分離すること。

## 4. コードの出力・修正フォーマット
* コードを修正・提示する際は、変更のないファイル全体を出力しないこと。
* 修正が必要な関数、または変更箇所周辺のコンテキスト（数行前後のコード）のみをピンポイントで出力し、どこを書き換えるべきかが一目でわかるようにすること。
* 不明点や仕様の矛盾を発見した場合は、勝手に推測して実装を進めず、必ず質問を提示して確認を取ること。