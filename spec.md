以下の仕様に基づき、ブラウザで動作するマルチプレイ対応の「3D戦闘機ドッグファイト＆レースゲーム」のシステム設計および必要なコードの実装を行ってください。360度自由に見渡せるフル3Dビューを前提とした設計としてください。

1. プロジェクト概要
ジャンル: 3D戦闘機フライトシューティング ＆ レースゲーム

プラットフォーム: Webブラウザ（PC向け）

プレイ人数: 最大8人（マルチプレイ）

2. 技術スタック
フロントエンド: HTML / CSS / JavaScript, Three.js（360度フル3Dビューの構築）

バックエンド: Node.js, WebSocket（Socket.io等を想定）

3. アーキテクチャ設計（同期と判定）
クライアント主導方式（Client-Authoritative）を採用する。

各クライアント（ブラウザ）のThree.js空間上で自機の移動・回転・当たり判定を計算し、その結果の座標やアクションをサーバーに送信して他プレイヤーと同期する。

射撃時のレスポンス（撃つ側の爽快感）を最優先とし、厳密なチート対策よりもプレイフィールを重視した設計とする。

4. クライアント機能・操作系
カメラ視点:

一人称視点（FPS / コックピット）と、三人称視点（TPS / 機体後方）をプレイ中に任意のキーでシームレスに切り替え可能にする。

操作方法（マウス＆キーボード）:

マウス: 機体のピッチ（上下）とヨー（左右）の精密な旋回・エイム操作。

キーボード: W/A/S/Dキー等を使用して、スロットル（加速・減速）および機体のロール（横回転）を制御する。

5. 機体のバリエーション（パラメータ設計）
プレイヤーはゲーム開始時（またはロビー内）で、性能の異なる以下の3種類の機体から1つを選択できる仕様とする。各機体は専用のパラメータ（速度、旋回性能、耐久力、武器性能など）を持つ。

Type A（バランス型 / オールラウンダー）:

全ての性能が平均的で扱いやすい。標準的な連射力と威力の機関砲を装備。

Type B（スピード・高機動型 / インターセプター）:

最高速度と加速力に優れ、ロール性能が高い。耐久力（HP）は低く、武器は一発の威力が低いが連射力に優れる（レースモードや一撃離脱に特化）。

Type C（重装甲・高火力型 / ファイター）:

耐久力（HP）が非常に高く、一発のダメージが大きい重機関砲（またはミサイル）を装備。最高速度や旋回性能が低く、もっさりとした挙動（ドッグファイトの乱戦や防衛に特化）。

6. ゲームモード
以下の2つのモードを実装し、ルーム作成時に選択可能とする。

レースモード: * 空間上に配置されたチェックポイントを順番に通過し、最速タイムや順位を競う。武器による攻撃は無効（または妨害程度）とする。

ドッグファイトモード:

制限時間内でより多くの敵機を撃墜したプレイヤー（またはチーム）が勝利するデスマッチ方式。

7. ロビー＆ルーム管理（マルチプレイ仕様）
プレイヤーがアクセスすると、まずロビー（待機画面）に接続される。

ルーム作成 / 参加: ホストがモード（レース or ドッグファイト）を選んでルームを作成し、他のプレイヤーは一覧から参加する。

柔軟な開始条件: 8人揃わなくてもホストの操作でゲームを開始できる。不足枠にはCPU（ボット）を追加可能とする。

チーム戦対応: ホストがロビー内で「チーム戦」を有効化できる。有効化されると、ロビー画面上で各プレイヤーが自ら所属するチーム（例：赤チーム / 青チーム）を選択・移動できるUIを提供する。

8. UI / HUD（ヘッドアップディスプレイ）
2Dレーダー: 画面右上等に円形または球体のレーダーを常時表示し、自機を中心とした敵機やチェックポイントの相対位置（上下左右）をマッピングする。

レース中のガイド: 画面端（エッジ）に、次のチェックポイントの方向を指し示す矢印ナビゲーションを表示する。

ドッグファイト中のターゲット表示: 画面内に敵機を捉えている場合、敵機の3Dモデルのすぐ上に「現在距離」と「HPバー」を直接オーバーレイ表示する（画面外の敵にはガイドを出さず、レーダーのみで追わせる）。

9. 出力要件
上記の仕様を実現するための、以下の項目を出力してください。

プロジェクトのディレクトリ構成案

クライアント側（Three.js）とサーバー側（WebSocket）の通信データ構造（機体タイプごとのパラメータ管理を含むJSONフォーマット等）

サーバー側（Node.js）のロビー・ルーム管理・チーム切り替えの基本ロジック

クライアント側のカメラ制御、およびマウス・キーボードによる飛行操作（ピッチ、ヨー、ロール）のコア実装コード